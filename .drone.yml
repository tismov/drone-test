kind: pipeline
type: docker
name: default

steps:
  - name: deploy_infra
    image: bitnami/kubectl:latest
    environment:
      test:
        from_secret: hello
    
    commands:
      - ehco $test
